# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# ~/.bitcoin/bitcoin.conf

# [core]
# Execute command when an alert (or long fork) is received (%s in cmd is replaced by message)
#alertnotify=
# Execute command when the best block changes (%s in cmd is replaced by block hash)
#blocknotify=
# Maintain a full transaction index, used by the getrawtransaction rpc call.
txindex=1

# [debug]
# Send trace/debug info to console instead of debug.log.
printtoconsole=1
# Log transaction fee per kB when mining blocks.
printpriority=1
# Run this node on the Bitcoin Test Network.
#testnet=1
# Run this node on its own independent test network.
regtest=1

# [rpc]
# Accept command line and JSON-RPC commands.
server=1
# Accept public REST requests.
#rest=1

# Username for JSON-RPC connections
#rpcuser=<user>
rpcuser=sxsw

# Password for JSON-RPC connections
#rpcpassword=<pw>
rpcpassword=sxsw

# Allow JSON-RPC connections from specified source. Valid for <ip> are a
# single IP (e.g. 1.2.3.4), a network/netmask (e.g.
# 1.2.3.4/255.255.255.0) or a network/CIDR (e.g. 1.2.3.4/24). This
# option can be specified multiple times
rpcallowip=0.0.0.0/0
